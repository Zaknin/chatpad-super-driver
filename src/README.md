# Source Directory

Modern implementation code will live here.

The preserved files under `legacy/source_release_0_0_4a/` are historical reference material only. No legacy binary is trusted, shipped, installed, loaded, or executed by this repository.

Initial implementation work should start with user-mode protocol parsing and fixture-backed tests before any Windows driver code is introduced.
